@model List<Hafta6.Models.Data>


<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">ID</th>
            <th scope="col">Email</th>
            <th scope="col">Username</th>
            <th scope="col">Password</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var user in Model){
            <tr>
                <td scope="col"><a asp-action="Duzenle" asp-route-id="@user.Id">Düzenle</a> | <a asp-action="Sil" asp-route-id="@user.Id">Sil</a></td>
                <td scope="col">@user.Id</td>
                <td scope="col">@user.Email</td>
                <td scope="col">@user.Username</td>
                <td scope="col">@user.Password</td>
            </tr>
        }
    </tbody>

</table>